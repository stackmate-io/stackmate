class Configuration {
  readonly path: string;

  constructor(path: string) {
    this.path = path;
  }

  async load() {
  }

  normalize() {
  }

  validate() {
  }
}

export default Configuration;
